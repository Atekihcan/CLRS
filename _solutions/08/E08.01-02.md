---
title:       Exercise 8.1-2
published:   2025-11-16 20:15
modified:    2025-11-16 20:15
keywords:    "Stirling's approximation, factorial, summation, asymptotic bounds"
description: "Obtain asymptotically tight bounds on lg(n!) without using Stirling's approximation. Instead, evaluate the summation."
---

> Obtain asymptotically tight bounds on $$\lg(n!)$$ without using Stirling's approximation. Instead, evaluate the summation $$\sum_{k=1}^{n} \lg k$$ using techniques from Section A.2.

We want to find tight bounds on $$\lg(n!)$$. Since $$n! = 1 \cdot 2 \cdot 3 \cdots n$$, taking logarithms gives us $$\lg(n!) = \sum_{k=1}^{n} \lg k$$.

To bound this summation, we'll split it into two parts and use integration techniques.

**Lower bound:**

Consider only the upper half of the sum:

$$\begin{align*}
\lg(n!) &= \sum_{k=1}^{n} \lg k \\
        &\geq \sum_{k=\lceil n/2 \rceil}^{n} \lg k \\
        &\geq \sum_{k=\lceil n/2 \rceil}^{n} \lg(n/2) \\
        &= (n - \lceil n/2 \rceil + 1) \lg(n/2) \\
        &\geq \frac{n}{2} \cdot (\lg n - 1) \\
        &= \frac{n \lg n}{2} - \frac{n}{2} \\
        &= \Omega(n \lg n)
\end{align*}$$

**Upper bound:**

We can bound each term from above using $$\lg k \leq \lg n$$ for $$k \leq n$$:

$$\begin{align*}
\lg(n!) &= \sum_{k=1}^{n} \lg k \\
        &\leq \sum_{k=1}^{n} \lg n \\
        &= n \lg n \\
        &= O(n \lg n)
\end{align*}$$

Combining both bounds, we get $$\lg(n!) = \Theta(n \lg n)$$.

{% capture note %}
This result confirms Theorem 8.1: any comparison sort requires $$\Omega(n \lg n)$$ comparisons in the worst case, since the decision tree must have at least $$n!$$ leaves and height at least $$\lg(n!) = \Theta(n \lg n)$$.
{% endcapture %}
{% include aside.html title='Connection to Sorting Lower Bound' %}
