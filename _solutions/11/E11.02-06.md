---
title:       Exercise 11.2-6
published:   2025-11-16 12:00
modified:    2025-11-16 12:00
keywords:    "random selection, expected time"
description: "Suppose we have stored n keys in a hash table of size m, with collisions resolved by chaining. Describe a procedure that selects a key uniformly at random from among the keys in the hash table and returns it in expected time O(L · (1 + 1/α))."
---

> Suppose we have stored n keys in a hash table of size m, with collisions resolved by chaining. Describe a procedure that selects a key uniformly at random from among the keys in the hash table and returns it in expected time O(L · (1 + 1/α)).

[Solution content to be added]
